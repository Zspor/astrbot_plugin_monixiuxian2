# 任务完成自检
- 代码/逻辑检查：确保新增指令或逻辑符合现有 async/handler 架构，保持中文命令常量一致。
- 配置/数据：如修改 config/*.json 或数据库字段，确认 MigrationManager 有对应升级步骤并更新 LATEST_DB_VERSION。
- 依赖：若新增第三方库，说明安装方式（pip）并确认 AstrBot 运行环境可用。
- 手动验证：在 AstrBot 中加载插件，跑通关键指令（创建角色、闭关、突破、装备、商店）进行冒烟；项目无自动化测试，需要人工验证。
- 文档：如有行为变化，更新 README 或备注相关命令/限制。